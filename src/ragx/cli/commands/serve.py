"""ragx serve api (placeholder)."""
from __future__ import annotations


def run(host: str = "0.0.0.0", port: int = 8000) -> None:
    print(f"[serve] Would start API on {host}:{port} (placeholder)")
